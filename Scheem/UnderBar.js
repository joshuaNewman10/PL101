//each


var _.identity = ['define', 'identity', ['x', 'x']];

var identity = 'define identity(x) {
	return x;
}'


var _.first = ['define', 'first', ['lambda', 'arr', 'n', '' ]];

var first = 'define first(arr, n) {
	if(n === undefined) {
		return 
	}
}'